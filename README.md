# OSD-war-trial-level

Objective:
1. Create a new branch called `flag-branch`
2. Create a file called `flag.txt`
3. Commit with message: "unlock"
4. Push the branch

The flag will appear after a successful push.
